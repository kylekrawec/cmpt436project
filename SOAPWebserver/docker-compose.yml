version: '3'

services:
  ProductCatalog:
    build:
      context: ./ProductCatalog
      dockerfile: Dockerfile
    image: productcatalog:1.0.0
    container_name: productcatalog
    ports:
      - "8080:80"